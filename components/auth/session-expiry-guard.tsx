"use client";

/**
 * With Supabase Auth, session management (access tokens, refresh tokens)
 * is handled automatically by the Supabase client + middleware.
 *
 * This component is kept as a no-op placeholder so existing imports
 * don't break.  It can be removed in a future cleanup.
 */
export function SessionExpiryGuard() {
  return null;
}
